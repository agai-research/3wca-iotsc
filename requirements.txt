# 3WCA-IoTSC - required at run time
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7
pyyaml>=6.0
scikit-learn>=1.3
networkx==3.1
python-docx>=1.1
pytest>=7.4

# Optional. Section 8 of the paper names these two libraries; this
# implementation ships its own pruned lattice builder (adjustment A10) and does
# not import them, so they are not needed to reproduce any result.
# concepts==0.9.2
# ray==2.8.0
